type Props = {
    appName: string;
    appUrl: string;
    resetUrl: string;
    to: string;
};
export declare const fillResetPasswordTemplate: ({ appName, appUrl, resetUrl, to, }: Props) => string;
export {};
